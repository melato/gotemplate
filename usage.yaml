short: substitute variables into a Go template  
use: [options] [template file...]
long: |
  uses Go text/template, to substitute values into a template.
  
  If there are no arguments, the template is parsed from the file specified
  with the -t <name> option.
  
  Otherwise, the template is parsed by parsing the argument files.
  The associated template executed is the one whose name is specified by
  -t <name> or otherwise, the first template.
  
  The -list option shows you the names of the parsed templates.
  
  The template data is constructed using options:
    -f [key=]{yaml-file}
    -json [key=]{json-file}
    -F key=file
    -D key=value  
  
    All keys used must be unique,
    so that the result will not depend on the options order